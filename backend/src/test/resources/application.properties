spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;NON_KEYWORDS=user;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS pianoml;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.datasource.username=sa
spring.datasource.password=sa
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.globally_quoted_identifiers=true
# Liquibase
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.default-schema=pianoml
spring.liquibase.enabled=true
spring.cloud.gcp.sql.enabled=false

# JWT — test-only key. Deterministic so token fixtures stay stable across runs.
# Must be >= 64 chars and must not collide with JwtProperties.FORBIDDEN_SECRETS.
jwt.secret=test-only-hmac512-key-do-not-use-outside-tests-0123456789abcdefghij
jwt.expiration-ms=3600000

spring.cloud.aws.region.static=us-east-1
spring.cloud.aws.credentials.access-key=dummy-access-key
spring.cloud.aws.credentials.secret-key=dummy-secret-key
aws.s3.bucket-name=pianoml-media
spring.cloud.aws.endpoint=https://dummy-endpoint.com/
