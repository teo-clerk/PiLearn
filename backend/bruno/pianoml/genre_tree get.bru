meta {
  name: genre_tree get
  type: http
  seq: 22
}

get {
  url: http://localhost:8080/genre_tree
  body: none
  auth: inherit
}

headers {
  Accept: application/json
  Authorization: Bearer {{token}}
}

script:pre-request {
  const token = bru.getEnvVar("token");
}
